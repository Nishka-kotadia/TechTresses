# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/freelancer-tax-buddy

# For production, use MongoDB Atlas or similar
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/freelancer-tax-buddy

# JWT Secret (if implementing authentication later)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Email Configuration (if implementing email features later)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
